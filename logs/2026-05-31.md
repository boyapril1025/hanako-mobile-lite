# 2026-05-31 Project Log

## Phase
Current phase: V0.1 delivery / source archival.

## Condensed Record
- Completed Phase 2 mobile web validation before Android shell work: session restore, foreground catch-up, and reconnect catch-up passed on the target phone browser.
- Created a native Kotlin + Gradle Android WebView shell under `android-shell/`.
- Fixed build environment around JDK 17, Android SDK 36.1, Gradle Wrapper 8.14.5, Android Gradle Plugin 8.13.2, and Aliyun Maven mirrors for Google/Gradle artifacts.
- Kept Android 16/API 36.1 as the V0.1 baseline. No fallback to API 35.
- Implemented a fixed WebView target URL: `http://192.168.31.11:14500/mobile/`.
- Implemented URL allowlist, WebView settings, cookie/DOM storage, native error overlay, retry button, back navigation, system bar inset padding, and main-frame error guarding.
- Built the debug APK successfully and installed it on the target phone.
- In-app A/B/C regressions passed:
  - A: refresh / WebView restart restores previous session.
  - B: background-to-foreground can catch up messages.
  - C: network reconnect can catch up messages.
- Removed visible refresh button attempts after overlap issues with the send button and workspace button.
- Replaced visible refresh controls with a bottom pull-up refresh gesture injected into the WebView page after load.
- Prepared an English-path repository copy at `G:/Hanako/workspace-changming/hanako-mobile-lite-v0.1` for GitHub upload.

## Completed
- Android WebView shell V0.1 debug build.
- Real phone installation and A/B/C validation.
- English project root and English directory names.
- Clean README and V0.1 release notes.
- Removed local-only Gradle distribution archive from the repository copy.
- Updated Gradle Wrapper to official Gradle 8.14.5 distribution URL.
- Removed Chinese-path-only Gradle override from the cleaned copy.
- Rebuilt from the English path successfully.

## Current State
- V0.1 is functionally accepted as a debug-stage lightweight Android shell.
- Source tree is ready for Git initialization and GitHub upload after repository target/visibility confirmation.

## TODO
- Confirm GitHub repository name and visibility.
- Push the prepared repository to GitHub.
- Optional later phases: configurable server address, release signing, app icon polish, file upload/download integration, push notifications, and background keepalive.

## Risks / Notes
- The target URL is hardcoded for the current local LAN host.
- Bottom pull-up refresh depends on scroll-container detection inside the WebView page.
- V0.1 uses debug signing only.
- File upload/download are intentionally out of scope.

## Next Entry Point
- Use `hanako-mobile-lite-v0.1/` as the canonical English-path project directory.
- Build command: `cd android-shell && .\gradlew.bat assembleDebug --no-daemon`.
- APK output: `android-shell/app/build/outputs/apk/debug/app-debug.apk`.
