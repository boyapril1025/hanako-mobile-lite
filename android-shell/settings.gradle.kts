pluginManagement {
    repositories {
        // Domestic mirrors first: Android Gradle Plugin uses Google Maven;
        // Kotlin plugin marker may resolve through Gradle Plugin Portal / public mirror.
        maven("https://maven.aliyun.com/repository/google")
        maven("https://maven.aliyun.com/repository/gradle-plugin")
        maven("https://maven.aliyun.com/repository/public")
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        // Domestic mirrors first for AndroidX / Google / Maven Central artifacts.
        maven("https://maven.aliyun.com/repository/google")
        maven("https://maven.aliyun.com/repository/public")
        google()
        mavenCentral()
    }
}

rootProject.name = "HanakoMobileShell"
include(":app")
